# Environment files
.env
.env.*
*.env
!.env.example

# Dependencies
node_modules/

# Build outputs
dist/
build/
.next/

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Personal AI tooling state — local skills, permissions, lockfiles.
# Commit .claude/settings.json (shared team policy) if/when you create it.
.claude/
!.claude/settings.json
.agents/
skills-lock.json

# Logs
logs/
*.log
npm-debug.log*

# Coverage
coverage/

# Temporary files
tmp/
temp/

# Ruby / fastlane (mobile release pipeline)
.bundle/
vendor/bundle/
mobile/ios/fastlane/report.xml
mobile/ios/fastlane/README.md
mobile/ios/fastlane/certs/
mobile/ios/fastlane/profiles/
mobile/ios/fastlane/.env*
mobile/android/fastlane/report.xml
mobile/android/fastlane/README.md
mobile/android/fastlane/.env*

# Signing material — must NEVER be committed
*.p8
*.p12
*.mobileprovision
*.keystore
!mobile/android/app/debug.keystore
mobile/android/app/keystore/

# Standalone repos — these folders have their own .git and remote (see CLAUDE.md):
#   backend/ → apcexchange/property360.git
#   mobile/  → apcexchange/property360-mobile.git
backend/
mobile/

# Presentation & content-creation artifacts — kept local, not committed.
# Marketing collateral, pitch decks, outreach copy, generated social
# images/banners, the content-gen skill, and brainstorm/browser scratch.
# These are collateral, not product code.
/docs/launch/
/web/public/launch/
/pitch-deck/
/web/marketing/
/.playwright-mcp/
/.superpowers/
/.claude/skills/property360-launch-content/
/_loader.png
